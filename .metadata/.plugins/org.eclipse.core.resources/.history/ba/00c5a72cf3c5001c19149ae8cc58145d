import org.apache.ivy.Main

class User {
	static void main() {
		File file = new File("new.txt")
		file.eachLine {
			line -> println "line : $line";
		}
		println("The file has {file.length()} bytes")
	}
}
